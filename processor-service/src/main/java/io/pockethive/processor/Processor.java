package io.pockethive.processor;

/**
 * Legacy processor implementation replaced by the Worker SDK runtime in Stage 4.
 * Retained only as a placeholder to avoid component scanning of the previous
 * Spring-managed component.
 */
final class Processor {
  private Processor() {
    // no-op
  }
}
