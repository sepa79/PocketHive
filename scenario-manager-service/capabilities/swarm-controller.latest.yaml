schemaVersion: "1.0"
capabilitiesVersion: "1.2"
role: swarm-controller
image:
  name: swarm-controller
ui:
  label: "Swarm Controller"
  color: "#f97316"
  shape: "square"
  abbreviation: "SC"
config:
  - name: trafficPolicy.bufferGuard.enabled
    type: boolean
    ui:
      label: Buffer guard enabled
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.queueAlias
    type: string
    ui:
      label: Queue alias
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.targetDepth
    type: integer
    ui:
      label: Target depth
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.minDepth
    type: integer
    ui:
      label: Min depth
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.maxDepth
    type: integer
    ui:
      label: Max depth
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.samplePeriod
    type: string
    ui:
      label: Sample period
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.movingAverageWindow
    type: integer
    ui:
      label: Moving average window
      group: Buffer guard
  - name: trafficPolicy.bufferGuard.adjust.maxIncreasePct
    type: integer
    ui:
      label: Max increase
      group: Adjust
  - name: trafficPolicy.bufferGuard.adjust.maxDecreasePct
    type: integer
    ui:
      label: Max decrease
      group: Adjust
  - name: trafficPolicy.bufferGuard.adjust.minRatePerSec
    type: number
    ui:
      label: Min rate
      group: Adjust
  - name: trafficPolicy.bufferGuard.adjust.maxRatePerSec
    type: number
    ui:
      label: Max rate
      group: Adjust
  - name: trafficPolicy.bufferGuard.prefill.enabled
    type: boolean
    ui:
      label: Prefill enabled
      group: Prefill
  - name: trafficPolicy.bufferGuard.prefill.lookahead
    type: string
    ui:
      label: Lookahead
      group: Prefill
  - name: trafficPolicy.bufferGuard.prefill.liftPct
    type: integer
    ui:
      label: Lift
      group: Prefill
  - name: trafficPolicy.bufferGuard.backpressure.queueAlias
    type: string
    ui:
      label: Queue alias
      group: Backpressure
  - name: trafficPolicy.bufferGuard.backpressure.highDepth
    type: integer
    ui:
      label: High depth
      group: Backpressure
  - name: trafficPolicy.bufferGuard.backpressure.recoveryDepth
    type: integer
    ui:
      label: Recovery depth
      group: Backpressure
  - name: trafficPolicy.bufferGuard.backpressure.moderatorReductionPct
    type: integer
    ui:
      label: Moderator reduction
      group: Backpressure
actions: []
panels: []
