package io.pockethive.generator;

/**
 * Legacy generator implementation replaced by the Worker SDK runtime in Stage 4.
 * The class is retained only as a placeholder to avoid classpath references to the
 * previous Spring component.
 */
final class Generator {
  private Generator() {
    // no-op
  }
}
