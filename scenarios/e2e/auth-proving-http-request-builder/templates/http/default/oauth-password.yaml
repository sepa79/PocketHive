serviceId: default
callId: oauth-password
protocol: HTTP
method: POST
pathTemplate: /api/auth-proof/request-builder/oauth-password
bodyTemplate: "{{ payload }}"
headersTemplate:
  content-type: application/json
authRef:
  profileId: oauth-password
  applyAs: HTTP_AUTHORIZATION_BEARER
