protocolVersion: "2.0.0"
id: auth-proving-http-request-builder
name: Auth Proving - HTTP Request Builder
description: Proves request-builder HTTP authRef application for OAuth, static
  bearer, basic, API key, query token, HMAC, and AWS signing.
template:
  image: swarm-controller:latest
  bees:
    - role: gen-oauth-client
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"oauth-client"}'
          headers:
            content-type: application/json
            x-ph-call-id: oauth-client
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-oauth-password
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"oauth-password"}'
          headers:
            content-type: application/json
            x-ph-call-id: oauth-password
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-static-bearer
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"static-bearer"}'
          headers:
            content-type: application/json
            x-ph-call-id: static-bearer
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-basic
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"basic-auth"}'
          headers:
            content-type: application/json
            x-ph-call-id: basic-auth
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-api-key
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"api-key"}'
          headers:
            content-type: application/json
            x-ph-call-id: api-key
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-query-token
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"query-token"}'
          headers:
            content-type: application/json
            x-ph-call-id: query-token
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-hmac
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"hmac"}'
          headers:
            content-type: application/json
            x-ph-call-id: hmac
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: gen-aws
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 0
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-http-request-builder","call":"aws"}'
          headers:
            content-type: application/json
            x-ph-call-id: aws
      work:
        out:
          out: build
      ports:
        - { id: out, direction: out }
    - role: request-builder
      image: request-builder:latest
      config:
        templateRoot: /app/scenario/templates/http
        serviceId: default
        passThroughOnMissingTemplate: false
      work:
        in:
          in: build
        out:
          out: proc
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: processor
      image: processor:latest
      config:
        baseUrl: "{{ sut.endpoints['default'].baseUrl }}"
        mode: THREAD_COUNT
        threadCount: 1
        inputs:
          type: RABBITMQ
        outputs:
          type: RABBITMQ
      work:
        in:
          in: proc
        out:
          out: post
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: postprocessor
      image: postprocessor:latest
      config:
        forwardToOutput: false
        txOutcomeSinkMode: NONE
        dropTxOutcomeWithoutCallId: true
      work:
        in:
          in: post
      ports:
        - { id: in, direction: in }

topology:
  version: 1
  edges:
    - id: e1
      from: { port: out, role: gen-oauth-client }
      to: { port: in, role: request-builder }
    - id: e2
      from: { port: out, role: gen-oauth-password }
      to: { port: in, role: request-builder }
    - id: e3
      from: { port: out, role: gen-static-bearer }
      to: { port: in, role: request-builder }
    - id: e4
      from: { port: out, role: gen-basic }
      to: { port: in, role: request-builder }
    - id: e5
      from: { port: out, role: gen-api-key }
      to: { port: in, role: request-builder }
    - id: e6
      from: { port: out, role: gen-query-token }
      to: { port: in, role: request-builder }
    - id: e7
      from: { port: out, role: gen-hmac }
      to: { port: in, role: request-builder }
    - id: e8
      from: { port: out, role: gen-aws }
      to: { port: in, role: request-builder }
    - id: e9
      from: { port: out, role: request-builder }
      to: { port: in, role: processor }
    - id: e10
      from: { port: out, role: processor }
      to: { port: in, role: postprocessor }
