serviceId: default
callId: token-key-collision-other
protocol: HTTP
method: POST
pathTemplate: /api/auth-proof/collision/other-should-not-send
bodyTemplate: "{{ payload }}"
headersTemplate:
  content-type: application/json
authRef:
  profileId: collision-two
  applyAs: HTTP_AUTHORIZATION_BEARER
