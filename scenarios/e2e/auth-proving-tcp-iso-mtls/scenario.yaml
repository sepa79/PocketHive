id: auth-proving-tcp-iso-mtls
name: Auth Proving - TCP ISO8583 mTLS
description: Proves TCP payload auth, ISO8583 MAC application, and
  processor-stage mTLS client certificate auth metadata.
template:
  image: swarm-controller:latest
  bees:
    - role: gen-tcp-prefix
      id: gen-tcp-prefix
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 1
        message:
          bodyType: SIMPLE
          body: 'PAYLOAD|tcp-prefix|AUTH-PROOF'
          headers:
            x-ph-call-id: tcp-prefix
      work:
        out:
          out: build-tcp
      ports:
        - { id: out, direction: out }
    - role: rb-tcp-prefix
      id: rb-tcp-prefix
      image: request-builder:latest
      config:
        templateRoot: /app/scenario/templates/tcp
        serviceId: default
        passThroughOnMissingTemplate: false
      work:
        in:
          in: build-tcp
        out:
          out: proc-tcp
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: proc-tcp-prefix
      id: proc-tcp-prefix
      image: processor:latest
      config:
        baseUrl: "{{ sut.endpoints['tcp-server'].baseUrl }}"
        tcpTransport:
          type: socket
          connectionReuse: NONE
          maxRetries: 1
          connectTimeoutMs: 2000
          readTimeoutMs: 3000
      work:
        in:
          in: proc-tcp
        out:
          out: post
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: gen-iso-mac
      id: gen-iso-mac
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 1
        message:
          bodyType: SIMPLE
          body: '{"scenario":"auth-proving-tcp-iso-mtls","call":"iso-mac"}'
          headers:
            x-ph-call-id: iso-mac
      work:
        out:
          out: build-iso
      ports:
        - { id: out, direction: out }
    - role: rb-iso-mac
      id: rb-iso-mac
      image: request-builder:latest
      config:
        templateRoot: /app/scenario/templates/tcp
        serviceId: default
        passThroughOnMissingTemplate: false
      work:
        in:
          in: build-iso
        out:
          out: proc-iso
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: proc-iso-mac
      id: proc-iso-mac
      image: processor:latest
      config:
        baseUrl: "{{ sut.endpoints['tcp-server'].baseUrl }}"
        tcpTransport:
          type: socket
          connectionReuse: NONE
          maxRetries: 1
          connectTimeoutMs: 2000
          readTimeoutMs: 3000
          maxBytes: 8192
      work:
        in:
          in: proc-iso
        out:
          out: post
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: gen-mtls
      id: gen-mtls
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 1
            maxMessages: 1
        message:
          bodyType: SIMPLE
          body: '{"sessionId":"MTLS-AUTH-PROOF","timestamp":"2026-05-08T00:00:00Z","encryptedData":"dummy","signature":"dummy"}'
          headers:
            x-ph-call-id: mtls-secure
      work:
        out:
          out: build-mtls
      ports:
        - { id: out, direction: out }
    - role: rb-mtls
      id: rb-mtls
      image: request-builder:latest
      config:
        templateRoot: /app/scenario/templates/tcp
        serviceId: default
        passThroughOnMissingTemplate: false
      work:
        in:
          in: build-mtls
        out:
          out: proc-mtls
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: proc-mtls
      id: proc-mtls
      image: processor:latest
      config:
        baseUrl: "{{ sut.endpoints['tcps-server'].baseUrl }}"
        tcpTransport:
          type: socket
          connectionReuse: NONE
          maxRetries: 1
          connectTimeoutMs: 5000
          readTimeoutMs: 8000
          sslVerify: false
      work:
        in:
          in: proc-mtls
        out:
          out: post
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: postprocessor
      id: postprocessor
      image: postprocessor:latest
      work:
        in:
          in: post
      ports:
        - { id: in, direction: in }

topology:
  version: 1
  edges:
    - id: e1
      from: { beeId: gen-tcp-prefix, port: out }
      to: { beeId: rb-tcp-prefix, port: in }
    - id: e2
      from: { beeId: rb-tcp-prefix, port: out }
      to: { beeId: proc-tcp-prefix, port: in }
    - id: e3
      from: { beeId: proc-tcp-prefix, port: out }
      to: { beeId: postprocessor, port: in }
    - id: e4
      from: { beeId: gen-iso-mac, port: out }
      to: { beeId: rb-iso-mac, port: in }
    - id: e5
      from: { beeId: rb-iso-mac, port: out }
      to: { beeId: proc-iso-mac, port: in }
    - id: e6
      from: { beeId: proc-iso-mac, port: out }
      to: { beeId: postprocessor, port: in }
    - id: e7
      from: { beeId: gen-mtls, port: out }
      to: { beeId: rb-mtls, port: in }
    - id: e8
      from: { beeId: rb-mtls, port: out }
      to: { beeId: proc-mtls, port: in }
    - id: e9
      from: { beeId: proc-mtls, port: out }
      to: { beeId: postprocessor, port: in }
