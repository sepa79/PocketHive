id: local-rest-with-multi-generators
name: Local REST - Multi Generators
description: Uses local images. Sample REST scenario that ships two generator
  bees with distinct payloads, rate limits, and an expected 10% beta generator
  failure rate.
template:
  image: swarm-controller:latest
  bees:
    - role: generator
      id: generator
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 30
        message:
          bodyType: HTTP
          path: /api/test-alpha
          method: POST
          body: hello-from-generator-a
      work:
        out:
          out: gen
      ports:
        - { id: out, direction: out }
    - role: generator
      id: generator-2
      image: generator:latest
      config:
        inputs:
          type: SCHEDULER
          scheduler:
            ratePerSec: 70
        message:
          bodyType: HTTP
          path: /api/test-beta
          method: POST
          body: hello-from-generator-b
      work:
        out:
          out: gen
      ports:
        - { id: out, direction: out }
    - role: moderator
      id: moderator
      image: moderator:latest
      config:
        mode:
          type: pass-through
          ratePerSec: 0.0
      work:
        in:
          in: gen
        out:
          out: mod
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: processor
      id: processor
      image: processor:latest
      config:
        baseUrl: "{{ sut.endpoints['default'].baseUrl }}"
        mode: THREAD_COUNT
        threadCount: 1
      work:
        in:
          in: mod
        out:
          out: final
      ports:
        - { id: in, direction: in }
        - { id: out, direction: out }
    - role: postprocessor
      id: postprocessor
      image: postprocessor:latest
      config:
        forwardToOutput: false
        txOutcomeSinkMode: NONE
        dropTxOutcomeWithoutCallId: true
      work:
        in:
          in: final
      ports:
        - { id: in, direction: in }

topology:
  version: 1
  edges:
    - id: e1
      from: { beeId: generator, port: out }
      to: { beeId: moderator, port: in }
    - id: e2
      from: { beeId: generator-2, port: out }
      to: { beeId: moderator, port: in }
    - id: e3
      from: { beeId: moderator, port: out }
      to: { beeId: processor, port: in }
    - id: e4
      from: { beeId: processor, port: out }
      to: { beeId: postprocessor, port: in }
