# http-sequence-six-auth-flow

Generated by PocketHive wizard from: Create an HTTP sequence proof with exactly six authenticated business requests using OAuth2 client credentials against local WireMock.

- Protocol: SEQUENCE
- Target: wiremock-local (http://wiremock:8080)
- Pattern: sequence
- Data source: SCHEDULER
- Default rate: 1/s
- Default duration: 20s
- Effective dispatch: one scheduler seed total (`maxMessages: 1`), which expands
  into the six sequence calls. The 20s profile duration does not mean sustained
  dispatch for 20 seconds.
- Traffic shape: smoke
- Auth: oauth2_client_credentials
- Requested dashboard metadata: `rtt_overview`; this is not runtime evidence.

## Generated Artifacts

- scenario.yaml
- variables.yaml
- sut/wiremock-local/sut.yaml
- authProfiles.yaml
- templates/http/sequence/start.yaml
- templates/http/sequence/profile.yaml
- templates/http/sequence/validate.yaml
- templates/http/sequence/session-update.yaml
- templates/http/sequence/confirm.yaml
- templates/http/sequence/receipt.yaml
- mock-config/wiremock/start.json
- mock-config/wiremock/profile.json
- mock-config/wiremock/validate.json
- mock-config/wiremock/session-update.json
- mock-config/wiremock/confirm.json
- mock-config/wiremock/receipt.json
- mock-config/wiremock/oauth-token-six-step.json

## Runtime Qualification Gate

Direct `scenario_deploy` and UI upload/Create do not load the seven files under
`mock-config/wiremock`. A future candidate that passes Connectivity must use
`workflow_deploy_start`, then `workflow_deploy_resume` through terminal
completion, and require `evidence.mockConfig.wiremock.loaded == 7` before
Start. Explicitly loading all seven supported mappings is the only equivalent;
direct deploy/Create is insufficient.

The postprocessor sets `txOutcomeSinkMode: NONE`, so this bundle does not
persist transaction outcomes to ClickHouse. Its `rtt_overview` dashboard and
`yes_for_nft_only` values are requested plan metadata, not qualification or
runtime evidence.
