# Scenario Plan Follow-ups

> Status: future / design
> Delivered baseline: `docs/archive/scenario-plan.md`

- [ ] Define additional step types beyond `config-update`, `start`, and `stop` in the canonical Scenario Plan contract before implementation.
- [ ] Decide whether environment/profile binding belongs in Scenario Plan or remains an explicit swarm-creation concern.
- [ ] Add UI support only after each new step has a canonical DTO, execution outcome, timeout behavior, and tests.

Network-profile steps are tracked separately in `docs/todo/network-proxy-followups.md`.
