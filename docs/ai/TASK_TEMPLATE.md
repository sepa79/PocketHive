# AI Task Template

**Goal**
- 

**Scope**
- 

**Constraints**
- Configure worker IO via `pockethive.inputs.<type>` / `pockethive.outputs.<type>` (or the matching `POCKETHIVE_INPUT_RABBIT_QUEUE` / `POCKETHIVE_OUTPUT_RABBIT_*` env vars); the legacy `pockethive.control-plane.queues.*` contract has been removed.

**Acceptance Criteria**
- 

**Deliverables**
- 

**Context**
- 
