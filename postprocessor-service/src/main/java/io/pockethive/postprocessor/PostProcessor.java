package io.pockethive.postprocessor;

/**
 * Legacy post-processor implementation replaced by the Worker SDK runtime in Stage 5.
 * Retained only as a placeholder to avoid component scanning of the former Spring bean.
 */
final class PostProcessor {
  private PostProcessor() {
    // no-op
  }
}
