---
- name: Remove PocketHive stack
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Reject remove until HiveForge owns Docker remove execution
      ansible.builtin.fail:
        msg: "PocketHive remove requires HiveForge-owned Docker remove execution; project Ansible has no Docker access in contract 0.5."
