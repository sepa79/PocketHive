spring:
  application:
    name: processor-worker-starter

pockethive:
  control-plane:
    exchange: ph.exchange
    swarm-id: sample-swarm
    worker:
      role: processor
      # Override with POCKETHIVE_CONTROL_PLANE_WORKER_INSTANCE_ID when deploying
      instance-id: ${POCKETHIVE_CONTROL_PLANE_WORKER_INSTANCE_ID:processor-1}
